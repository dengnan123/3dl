import * as path from 'path';

export const staticFilePath = '';

export const screenBuildPath = path.resolve(
  __dirname,
  './nodeShells/plugin.ts',
);

export const libsBuildPath = path.resolve(__dirname, './nodeShells/plugin.ts');
